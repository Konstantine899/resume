# Tasks: Image Component Improvement

**Change**: image-improvements · **Date**: 2026-08-09
**Source**: `image-improvement-spec.md` (IMG-01..10) + `image-improvement-design.md` (decisions 1–6)
**Gate**: 68 existing tests in `Image.test.tsx` (~650 lines) pass UNCHANGED (behavioral-noop). 5 consumers untouched (Avatar, AvatarImage, AvatarAbout, AvatarHero, CardImage). 22 stories keep rendering.

## Review Workload Forecast

| Field                   | Value                                                                         |
| ----------------------- | ----------------------------------------------------------------------------- |
| Estimated changed lines | ~450–650 (`additions + deletions`)                                            |
| 400-line budget risk    | High                                                                          |
| Chained PRs recommended | Yes                                                                           |
| Suggested split         | PR 1 hygiene → PR 2 correctness+i18n → PR 3 plays+docs → PR 4 hook (OPTIONAL) |
| Delivery strategy       | ask-on-release                                                                |
| Chain strategy          | pending (ask user)                                                            |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
```

### Suggested Work Units

| Unit         | Goal                                                   | PR   | Focused test command                                                  | Runtime harness                                                                         | Rollback                                                               |
| ------------ | ------------------------------------------------------ | ---- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 1            | Hygiene: named-only, dead exports, gate-free validator | PR 1 | `npm run analyze:dead-code`; `npx vitest run src/shared/ui/Image`     | `npm run analyze:dead-code` + `type-check:strict`; `npx vitest run src/shared/ui/Image` | revert commit; re-export/default restore trivial, zero consumer impact |
| 2            | Correctness: srcSet fix + i18n + aria (RED first)      | PR 2 | `npx vitest run src/shared/ui/Image`                                  | `npm run type-check:strict`; full vitest (68-gate)                                      | revert commit; single-file `Image.tsx` + 2 locale keys revert cleanly  |
| 3            | Plays + docs                                           | PR 3 | `npm run storybook:test`                                              | Storybook screenshot diff (Card/Modal boxes)                                            | revert docs commit; plays additive                                     |
| 4 (OPTIONAL) | `useImageRender` hook (IMG-10)                         | PR 4 | `npx vitest run src/shared/ui/Image/lib/hooks/useImageRender.test.ts` | full vitest (noop gate); drop-with-note allowed by spec                                 | revert restores inline memo/callback (identical values)                |

Chain strategy: `pending` — ask user at apply (stacked-to-main vs feature-branch-chain).

## Tasks

| ID      | Req        | Batch       | Title                               | Files                                                                                         | Work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Acceptance                                                                                                                                                                                                                       | Deps                                                                                                                                                               |
| ------- | ---------- | ----------- | ----------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| [x] T1  | IMG-01     | 1           | Named-only exports                  | `ui/Image.tsx:243`, `lib/hooks/useImageLoading.ts:192`, `lib/utils/imageValidation.ts:169`    | Remove the 3 `export default` (Image, useImageLoading, validatePropPathProps). Named exports remain; zero default-import consumers (grep-verified)                                                                                                                                                                                                                                                                                                                                           | no `export default` in the 3 files; `type-check:strict` + consumers compile                                                                                                                                                      | ✅ 6016e31                                                                                                                                                         |
| [x] T2  | IMG-02     | 1           | Dead export removal (re-lock)       | `model/constants.ts`, `index.ts`, `lib/hooks/useImageLoading.ts`                              | Delete knip-listed dead exports: `useImageLoadingSimple` (hook:175), `PLACEHOLDER_CONFIG` (`:79`), `SUPPORTED_IMAGE_TYPES` (`:125`), `ARIA_DEFAULTS` (`:159`), `IMAGE_CONSTANTS` (aggregator `:173`); drop the 5 re-exports from `index.ts`. **Re-run `analyze:dead-code` at implementation to re-lock EXACT list** — never remove consumed (IMAGE_DEFAULTS, INTERSECTION_OBSERVER_CONFIG, IMAGE_SIZE_VALUES, IMAGE_VARIANT_RADIUS, enum lists + VALIDATION_MESSAGES for imageValidation.ts) | `analyze:dead-code` no longer lists the 5 names + 3 defaults; type-check 0; dev-validators keep importing enum lists                                                                                                             | ✅ 6016e31                                                                                                                                                         |
| [x] T3  | IMG-03     | 1           | Validator internal-only             | `index.ts`, `lib/utils/imageValidation.ts`                                                    | Remove `validateImageProps`/`normalizeImageProps`/`logValidationWarnings` from public `index.ts:37-41`; internals import direct from `lib/utils/imageValidation`; keep dev-guard in `logValidationWarnings` + explicit `isDevelopment` flag on `validateImageProps` unchanged                                                                                                                                                                                                                | 3 names absent from public index; direct-path imports resolve; `console.warn` behavior unchanged (dev only)                                                                                                                      | ✅ 6016e31                                                                                                                                                         |
| [x] T4  | IMG-04     | 2           | RED: object-src srcSet tests        | `ui/Image.improvements.test.tsx` (new)                                                        | Write failing tests: `src={{src, srcSet}}` → `<img>` gets BOTH `src="/a.jpg"` and `srcset="…"`; object WITHOUT `srcSet` → src only, NO srcset attr; string `src` + native `srcSet`/`sizes` passthrough verbatim                                                                                                                                                                                                                                                                              | srcSet tests FAIL on current code (`:81,219` drops it) + the no-srcSet test FAILS (currently `srcset` never set) — RED                                                                                                           | —                                                                                                                                                                  |
| [x] T5  | IMG-04     | 2           | object-src srcSet fix               | `ui/Image.tsx`                                                                                | `const resolvedSrc = typeof src === 'string' ? { src, srcSet: undefined } : src;` → `<img src={resolvedSrc.src} srcSet={resolvedSrc.srcSet} …>` — srcSet BEFORE `{...restProps}` so explicit native srcSet/sizes still win                                                                                                                                                                                                                                                                   | T4 tests GREEN; `Image.test.tsx:501-506` (src-only assert) green; 68-gate; string path unchanged                                                                                                                                 | T4                                                                                                                                                                 |
| [x] T6  | IMG-06     | 2           | i18n fallback                       | `ui/Image.tsx:151`, `src/shared/lib/i18n/locales/en.json`, `ru.json`                          | Replace hardcoded `Image not available` → `t('imageNotAvailable')` via `useTranslation()`; add flat key `imageNotAvailable` (en "Image not available", ru "Изображение недоступно"); fallback prop (string/ReactNode) escape-hatch stays untranslated                                                                                                                                                                                                                                        | en fallback text; `i18n.changeLanguage('ru')` → ru text; custom fallback verbatim; locale keys flat (no nesting)                                                                                                                 | T5                                                                                                                                                                 |
| [x] T7  | IMG-08     | 2           | aria-describedby target             | `ui/Image.tsx:144-168,231`                                                                    | `fallbackDescriptionId = isError && !decorative ? \`image-${alt                                                                                                                                                                                                                                                                                                                                                                                                                              |                                                                                                                                                                                                                                  | 'error'}-error\` : undefined`; attach the id to the fallback node in ALL 3 render-fallback branches (img/div/default); keep `aria-describedby` only in error state | `document.getElementById('image-Photo-error')` exists after `fireEvent.error`; absent on load; `Image.test.tsx:533-541` aria-truthy gate green | T5  |
| [x] T8  | IMG-07     | 3           | 6 Storybook plays                   | `ui/Image.stories.tsx`                                                                        | `play` via `@storybook/test` `within`/`expect` + `waitFor` where async: `Default` (role=img + `figure[data-loading]`), `Rounded` (`data-variant=rounded`), `SizeSmall` (`data-size=sm` + container width 64px), `ErrorWithFallback` (fallback node + `aria-describedby` truthy), `PlaceholderSkeleton` (skeleton during initial phase), `LoadingModes` (`loading=eager` vs `loading=lazy`)                                                                                                   | `npm run storybook:test` all 6 plays green; no story layout breakage                                                                                                                                                             | T5, T7                                                                                                                                                             |
| [x] T9  | IMG-09     | 3           | Docs contract section               | `docs/specs/ui-kit-contract.md`                                                               | Add `### Image specifics` next to Paragraph/Link/Icon/Divider: props summary (src union, alt, 4 variants, 4 sizes, 5 objectFits, placeholders, lazyModes, fallback, decorative, priority, forceLoading), loading hook, 5 consumers, 22 stories + 6 plays, knip-clean exports                                                                                                                                                                                                                 | section present & consistent with IMG-07/10; inventory Image row updated                                                                                                                                                         | T8                                                                                                                                                                 |
| ~~T10~~ | ~~IMG-10~~ | ~~4 (OPT)~~ | **DEFERRED**: `useImageRender` hook | `lib/hooks/useImageRender.ts` (new), `lib/hooks/useImageRender.test.ts` (new), `ui/Image.tsx` | Move the 5 inline `useMemo` + 2 `useCallback` verbatim into `useImageRender(props, { loadingStatus, isError })` returning the same values + handleLoadSuccess/Error; thin component                                                                                                                                                                                                                                                                                                          | **Dropped-with-note at apply (user decision 2026-08-09)**: all CRITICAL requirements closed; hook is structural-only refactor with zero behavior; 68-gate already green. Spec allows drop-with-note.                             | —                                                                                                                                                                  |
| [x] T11 | —          | verify      | Final gate                          | (none)                                                                                        | Full gate + screenshot diff                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅ `type-check:strict` 0; `lint` Image-files EXIT 0 (16 pre-existing errors in `common.ts`, not ours); `npx vitest run src/shared/ui/Image` 76/76; `analyze:dead-code` Image names clean; `storybook:test` 52 suites / 618 tests | T1..T10                                                                                                                                                            |

**Deviation note**: T4 (RED tests) placed BEFORE T5 (impl) per TDD, honoring tests-first over batched listing order (Link/Icon precedent). Gate count: the incoming task instruction cited "65 existing tests", but both spec and design verify 68 assertions in `Image.test.tsx` (~650 lines) — 68 is the authoritative gate count (spec §Test: 68 existing + 5–8 new).

## Implementation Order

Batch 1 hygiene (T1→T3) → Batch 2 correctness+i18n (T4 RED → T5→T7) → Batch 3 stories+docs (T8→T9) → Batch 4 OPTIONAL hook (T10) → T11 final verify. Each batch = one chained PR per Work Units above. Rollback per unit surgical (`git revert <unit-commit>`): IMG-01/02/03 re-export restores trivially; IMG-04/08 single-file `Image.tsx` revert; IMG-06 locale keys revert 2 lines; IMG-07/09 revert docs.

## Summary

| Batch            | Tasks | Focus                                                 | PR   |
| ---------------- | ----- | ----------------------------------------------------- | ---- |
| Hygiene          | T1–T3 | named-only, dead exports, gatekeeper                  | PR 1 |
| Correctness+i18n | T4–T7 | srcSet, translated fallback, aria binding (RED-first) | PR 2 |
| Stories+docs     | T8–T9 | 6 plays, contract section                             | PR 3 |
| Hook (OPT)       | T10   | useImageRender                                        | PR 4 |
| Verify           | T11   | full gate + screenshots                               | —    |

**Decision needed before apply**: Yes — chain strategy `pending`; ask user at apply (stacked-to-main vs feature-branch-chain). ~450–650 lines → 400-line budget risk: High → chained PRs recommended per Work Units.
