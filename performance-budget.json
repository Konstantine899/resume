{
  "version": "1.0.0",
  "description": "Performance budgets for bundle size enforcement",
  "budgets": {
    "chunks": {
      "default": {
        "maxSize": 120000,
        "maxSizeGzip": 30720,
        "description": "Default chunk size limit (117kb raw, 30kb gzip)"
      },
      "vendor": {
        "maxSize": 204800,
        "maxSizeGzip": 61440,
        "description": "Vendor chunk limit (200kb raw, 60kb gzip)"
      },
      "main": {
        "maxSize": 180000,
        "maxSizeGzip": 49152,
        "description": "Main chunk limit (176kb raw, 48kb gzip)"
      }
    },
    "total": {
      "maxSize": 512000,
      "maxSizeGzip": 153600,
      "description": "Total bundle size limit (500kb raw, 150kb gzip)"
    },
    "assets": {
      "maxSize": 120000,
      "description": "Individual asset limit (117kb)"
    }
  },
  "patterns": {
    "vendor": ["node_modules", "vendor"],
    "main": ["index", "main", "app"],
    "chunk": [".chunk."]
  },
  "thresholds": {
    "warning": 0.8,
    "error": 1.0
  },
  "enforcement": {
    "failBuild": true,
    "showDetails": true,
    "compareWithPrevious": true
  }
}
